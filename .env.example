# Gemini API key (https://aistudio.google.com/apikey)
GEMINI_API_KEY=

# Vercel Blob read/write token (from the Blob store in your Vercel project settings)
# Needed starting Phase 2, once document uploads are relayed through Blob.
BLOB_READ_WRITE_TOKEN=
